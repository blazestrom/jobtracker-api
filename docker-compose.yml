version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: jobtracker-db
    environment:
      POSTGRES_DB: jobtracker_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build: .
    container_name: jobtracker-app
    ports:
      - "8080:8080"
    depends_on:
      db:
        condition: service_healthy
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://db:5432/jobtracker_db
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: password
      JWT_SECRET: ThisIsMySecretKeyForJWTTokenGenerationPleaseChangeInProduction123456
      JWT_EXPIRATION: "86400000"
    restart: always

volumes:
  pgdata:
